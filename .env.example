# Local development (with docker compose up -d)
DATABASE_URL="postgresql://iberic:iberic@localhost:5432/iberic"

# Production / Coolify — paste the internal Postgres URL from Coolify
# DATABASE_URL="postgresql://user:password@host:5432/dbname"

NEXTAUTH_SECRET="change-me-to-a-random-secret-in-production"
NEXTAUTH_URL="http://localhost:3000"
# Production: public URL for emails/tracking (overrides NEXTAUTH_URL when set)
# APP_URL="https://ibericos.enviaclientes.com"

# Optional: tracking emails and catalog notifications
RESEND_API_KEY=""
EMAIL_FROM="Iberic Distributions <noreply@ibericdistributions.com>"
